name: selfhosted-runner
on:
 push 
jobs:
 selfhosted-runner:
  name: self-hosted
  runs-on: self-hosted
  steps:
   - name: testing self-hosted-runner
     run: |
             echo "Welcome to Self-Hosted runner"
             echo "OK !!!"
 
 
